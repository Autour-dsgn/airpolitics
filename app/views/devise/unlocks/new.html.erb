<div class="container">
  <div class="form">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <h3 id="formtitle"><h3><strong>Renvoyer les instructions de déverrouillage</strong></h3>

          <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
            <%= f.error_notification %>
            <%= f.full_error :unlock_token %>

            <div class="form-inputs">
              <%= f.input :email, placeholder: 'jeandupont@gmail.com',
                          required: true,
                          autofocus: true,
                          input_html: { autocomplete: "email" } %>
            </div>

            <div class="form-actions">
              <%= f.button :submit, "Envoyer les instructions", class: "btn btn-primary" %>
            </div>
            <br>
          <% end %>
          <%= render "devise/shared/links" %>
       </div>
    </div>
  </div>
</div>
