<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" data-controller="navbar" data-action="scroll@window->navbar#updateNavbar">
  <div class="container container-fluid">
    <a class="navbar-brand" href="<%= root_path %>", width: 100% >
      <%= image_tag("logo.svg") %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
          <li class="nav-item active">
            <a class="btn btn-outline-primary btn-sm" id="nav-btn" href="<%= new_politician_path %>">Proposez vos services</a>
          </li>
          <li class="nav-item">
            <!-- Lien à mettre à jour / présence si login -->
            <% if current_user.present? %>
              <a class="nav-link" href="#">Lucas Delpino<!--<%= current_user.email %> --></a>
            <% else %>
              <a class="nav-link" href="#"> </a>
            <% end %>
          </li>
          <li class="nav-item dropdown">
             <!-- Avatar à mettre à jour / présence si login -->
              <% if current_user.present? %>
              <img class="avatar dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="https://ca.slack-edge.com/T02NE0241-U02TB5KUU85-27b27660d58d-512" />
              <% else %>
              <img class="avatar dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="https://aui.atlassian.com/aui/8.8/docs/images/avatar-person.svg" />
              <% end %>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <!-- Liens à mettre à jour / présence si non login -->
              <% if current_user.present? %>
              <a class="dropdown-item" href="<%= my_bookings_path %>">Mes réservations</a>
              <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item"%>
              <% else %>
              <a class="dropdown-item" href="<%= new_user_registration_path %>">Inscription</a>
              <a class="dropdown-item" href="<%= new_user_session_path%>">Connexion</a>
              <% end %>

            </div>
          </li>
      </ul>
    </div>
  </div>
</div>
